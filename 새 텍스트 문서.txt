webcontent -> new -> html

프로젝트선택 -> run as

항상 톰캣사용 체크


많은 종류의 브라우저와 서버를 연결하는법
=> 대화방법 (protocol) => 웹서비스프로토콜 : HTTP
=> 이 방식으로 통일하기로 약속하자

URL 약속
서버 / 프로젝트 / 클래스


서버는 메모리를 아끼기 위해 로그인처리후에 연결을 끊어버림
=> 로그인을 계속 반복해야하는 불편함.

쿠키발급(sessionID) 하면서 session 객체를 만들어 정보를 기록
(db로 보내는게 아니고 메모리에 임시적으로 30분 유지(default값))

그 후 사용자가 다시 접속했을때 세션객체에 정보가 있는지 확인하고
바로 로그인 처리
